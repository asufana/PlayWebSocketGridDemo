# Application dependencies

require:
    - play
    - commons-validator -> commons-validator 1.3.1
    - com.google.code.gson -> gson 2.1
    - ddd -> ddd

repositories:
    - My modules:
        type:       local
        artifact:   ${application.path}/localModules/[module]
        contains:
            - ddd

